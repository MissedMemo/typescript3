{
  "scripts": {
    "build": "tsc --target ES2018 src/*.ts --outDir dist",
    "runIt": "lite-server --baseDir='dist'",
    "start": "yarn build && yarn runIt"
  },
  "devDependencies": {
    "lite-server": "^2.4.0",
    "typescript": "^3.2.2"
  }
}
